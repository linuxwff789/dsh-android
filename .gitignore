# Rootfs is bootstrapped on-device (bootstrap.sh + pkglist.txt), not bundled.
# No *.tar.xz is committed (bootstrap downloads .debs directly from TUNA).
*.tar.xz

# Local build outputs
dist/

# CodeGraph index
.codegraph/

# keystore private material (only the .p12 is committed for CI signing)
keystore/key.pem
keystore/cert.pem
