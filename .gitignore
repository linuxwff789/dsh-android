# Large runtime assets are not committed to git; stage them locally or let CI
# download them (see .github/workflows/build-apk.yml)
app/src/main/assets/opt/dsh/rootfs.tar.xz
*.tar.xz

# Local build outputs
dist/

# keystore private material (only the .p12 is committed for CI signing)
keystore/key.pem
keystore/cert.pem