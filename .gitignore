# Old full-rootfs approach is deprecated; keep only the small bundled base.
app/src/main/assets/opt/dsh/rootfs.tar.xz

# Allow the small (28MB) bundled base — it is committed so CI can build it in.
*.tar.xz
!app/src/main/assets/opt/dsh/base.tar.xz

# Local build outputs
dist/

# keystore private material (only the .p12 is committed for CI signing)
keystore/key.pem
keystore/cert.pem